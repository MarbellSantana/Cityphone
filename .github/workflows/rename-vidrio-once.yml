name: Rename Vidrio Once

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/rename-vidrio-once.yml'

jobs:
  rename:
    runs-on: ubuntu-latest
    steps:
      - name: Rename inventory items
        run: |
          set -euo pipefail
          curl --fail --show-error --silent \
            'https://yftaloxtylijudnoewrm.supabase.co/functions/v1/cityphone-maintenance'
